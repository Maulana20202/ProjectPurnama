using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TitikJalan : MonoBehaviour
{

    [SerializeField] public Transform[] titikPoint1;
    [SerializeField] public Transform[] titikPoint2;
    
    // Start is called before the first frame update
    void Awake()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
