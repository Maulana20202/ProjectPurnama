using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PengecekKursiKosong : MonoBehaviour
{
    [Header("Bool Pengecek Kursi")]
    public bool Kursi_1;
    public bool Kursi_2;
    [Header("Bool Pengecek Apakah Aktif")]
    public bool Kursi_1_Aktif;
    public bool Kursi_2_Aktif;


    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
